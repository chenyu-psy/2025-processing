# Presentation

This folder contains a standalone Quarto presentation project for HTML slides.

## Files

- `_quarto.yml`: project configuration for rendering slides
- `slides.qmd`: main presentation source file
- `slides.css`: presentation-specific styling

## How to use

From the repository root, run:

```bash
quarto render presentation
```

The rendered HTML slides will be written to `presentation/output/`.
